{
  "name": "back-nd",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "start:dev": "ts-node -r tsconfig-paths/register src --files",
    "start:watch": "npm run watch:dev",
    "start:debugger": "node --inspect --require ts-node -r tsconfig-paths/register src/index.ts",
    "watch:dev": "nodemon --watch src -e ts,tsx --exec npm run start:dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "qiangang",
  "license": "ISC",
  "dependencies": {
    "koa": "^2.13.0",
    "koa-router": "^9.4.0"
  },
  "devDependencies": {
    "@types/koa": "^2.11.6",
    "@types/koa-router": "^7.4.1",
    "nodemon": "^2.0.6",
    "ts-node": "^9.0.0",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.0.5"
  }
}
